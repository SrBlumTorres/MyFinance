// import pg from 'pg';

// const pool = new pg.Pool({
//   connectionString: process.env.DDBB_URL
// });

// async function query(query = '', values?: any[]){
//     const results = await pool.query(query, values);
//     return results.rows;
// }

// export default query;